interface io_stream
{
    typedef sequence<octet> data_t;

    unsigned long read (
        in  unsigned long count,
        out data_t data);

    unsigned long write (
        in  unsigned long count,
        in data_t data);
}